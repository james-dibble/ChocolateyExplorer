<UserControl x:Class="ChocolateyExplorer.WPF.Views.InstalledPackages"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             DataContext="{Binding Source={StaticResource Locator}, Path=InstalledPackages}">
    <DockPanel LastChildFill="true">
        <ListView ItemsSource="{Binding InstalledPackages}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Package Id" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Version" DisplayMemberBinding="{Binding Version}" />
                    <GridViewColumn />
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
